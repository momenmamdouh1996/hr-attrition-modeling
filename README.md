# hr-attrition-modeling
A machine learning project that predicts employee turnover using classification models. Built on an HR Analytics dataset (1,470 records), it covers full EDA, preprocessing, and model comparison across Logistic Regression, Random Forest, and XGBoost — evaluating each on accuracy, precision, and recall to select the best performer.
